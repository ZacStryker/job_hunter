CREATE TABLE `profile` (
	`id` integer PRIMARY KEY AUTOINCREMENT NOT NULL,
	`name` text,
	`email` text,
	`phone` text,
	`location` text,
	`linkedin_url` text,
	`github_url` text,
	`summary` text,
	`experience` text,
	`skills` text,
	`education` text
);
